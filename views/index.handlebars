<div class="col" id="left">
	<ul>
		{{#each tasks}}
			{{#unless done}}
				{{> tasks/todo }}
			{{/unless}}
		{{/each}}
	</ul>
</div>

<div class="col" id="right">
	<ul>
		{{#each tasks}}
			{{#if done}}
				{{> tasks/done }}
			{{/if}}
		{{/each}}
	</ul>
</div>




